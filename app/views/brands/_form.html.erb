<%= form_for(@brand) do |f| %>
  <% if @brand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

      <ul>
      <% @brand.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :brand_category_id %><br />
    <%= f.number_field :brand_category_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<ul>
<% BrandCategory.all.each do |category|%>
	<li><%=category.name%> <%=category.id%></li>
<% end %>
</ul>