<!DOCTYPE html>
<html>
<head>
  <title>Market Img</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>

<body>		
	<header id="branding">
		<div class="col-width">		
			<hgroup id="logo">
				<h1 id="site-title">
					<%= link_to "market.img", root_path %>
				</h1>
			</hgroup>			
		</div>
	</header>
	<div id="main">
		<% if logged_in? %>
		<%= link_to current_user.username + " 로그아웃", logout_path %>
		<% else %>
			<%= link_to "로그인", login_path%>
		<% end %>
		<div class="col-width"> 			   
				<%= yield %>
		</div>
	</div>
	<footer id="colophon">
		<div class="col-width">
		</div>	
	</footer>
</body>
</html>
