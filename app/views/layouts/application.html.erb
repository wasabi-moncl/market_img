<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "MarketImg" %></title>
  <%= csrf_meta_tags %>
  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
</head>
<body>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/">MarketImg</a>
				<div class="container nav-collapse">
					<ul class="nav pull-right">
						<li><%= link_to "템플릿 관리", templates_path if logged_in? && (current_user.username == "admin" || current_user.username == "moncl") %></li>
				 		<% if logged_in? %>
							<li><%= link_to current_user.username + " 로그아웃", logout_path %></li>
							<li><%= link_to current_user.username + " 정보수정", edit_user_path(current_user) %></li>
				 		<% else %>
				 			<li><%= link_to "로그인", login_path%></li>
				 		<% end %>
					</ul>
				</div><!--/.nav-collapse -->
      </div>
    </div>
  </div>

  <div class="container">
    <div class="content">
			<%= render 'layouts/messages' %>
      <%= yield %>
    </div><!--/content-->

    <footer>
      <p>&copy;2012</p>
    </footer>

  </div> <!-- /container -->



</body>
</html>
