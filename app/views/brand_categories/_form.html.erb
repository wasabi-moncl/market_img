<%= form_for(@brand_category) do |f| %>
  <% if @brand_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand_category.errors.count, "error") %> prohibited this brand_category from being saved:</h2>

      <ul>
      <% @brand_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
